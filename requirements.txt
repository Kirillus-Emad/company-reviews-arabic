# ── Arabic NLP ──────────────────────────────────────────────────────────────
camel-tools==1.6.0
pyspellchecker==0.8.3
contractions==0.1.73
emoji==2.14.1
nlpaug==1.1.11

# ── General NLP ─────────────────────────────────────────────────────────────
nltk==3.9.1

# ── Machine Learning ────────────────────────────────────────────────────────
scikit-learn==1.8.0
xgboost==3.1.1

# ── Data & Math ─────────────────────────────────────────────────────────────
numpy==2.1.3            # camel-tools requires >=2.0; do not pin exact version
pandas==2.2.3
scipy==1.15.2

# ── Visualisation ───────────────────────────────────────────────────────────
matplotlib==3.10.1
seaborn==0.13.2

# ── Utilities ───────────────────────────────────────────────────────────────
tqdm==4.67.1
joblib==1.4.2
gradio>=4.0.0

# ── Transformer training ─────────────────────────────────────────────────────
# torch is provided by the runtime (Colab / local CUDA env) — do NOT pin it here.
# Pinning torch==x.y.z downgrades Colab's pre-installed version and breaks torchvision.
transformers==4.57.6
accelerate>=0.26.0         # required by HuggingFace Trainer
peft>=0.10.0               # LoRA adapters for XLM-RoBERTa Large
torchao>=0.16.0            # peft requires >0.16.0; Colab ships 0.10.0 which conflicts
tokenizers>=0.21
huggingface-hub==0.34.4
safetensors>=0.5.3

# ── Transitive deps that need explicit pinning ───────────────────────────────
# camel-tools Arabic NLP chain
cachetools==6.0.0
alyahmor==0.2
arramooz-pysqlite==0.4.2
editdistance==0.8.1
libqutrub==1.2.4.1
marisa-trie==1.2.1
muddler==0.1.3
mysam-tagmanager==0.4
Naftawayh==0.4
PyArabic==0.6.15
pyahocorasick==2.1.0
qalsadi==0.5
Tashaphyne==0.3.6
langcodes==3.5.0
language_data==1.3.0

# contractions chain
textsearch==0.0.24
anyascii==0.3.2